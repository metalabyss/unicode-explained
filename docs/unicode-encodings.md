3. Подробнее о семействе кодировок Unicode
==========================================

Как уже упомянуто, стандарт Unicode не только определяет кодовое пространство символов и их свойств, но и описывает алгоритмы для представления этих символов в памяти. Так, в стандарте определено семейство из нескольких кодировок (англ. _Unicode Transformation Format, UTF_), в числе которых UTF-8 и UTF-16.

3.1. UTF-8. Общие сведения
--------------------------

В UTF-8 кодовые позиции представляются блоками по 8 бит (будем называть такие блоки **словами**). В данной кодировке отдельно взятая кодовая позиция может быть представлена последовательностью от 1 до 4 слов. UTF-8 — кодировка с переменной длиной кода, и это свойство позволяет компактно хранить закодированный текст.

3.2. UTF-8. Алгоритм кодирования
--------------------------------

**Шаг 1.** Если кодируемая позиция лежит в диапазоне ASCII нулевой плоскости, то ее код имеет вид «0xxx xxxx», где «xxx xxxx» — бинарное представление кодовой позиции. Таким образом, ее представление в памяти займет 1 байт, и ее кодирование на этом окончено.

В ином случае нужно определить количество слов, необходимых для хранения закодированной кодовой позиции. Это количество определяется в зависимости от числа значащих бит в ее бинарном представлении:

| Количество значащих бит (макс.) | Количество слов |
|----|---|
| 7  | 1 |
| 11 | 2 |
| 16 | 3 |
| 21 | 4 |

Помимо таблицы для определения максимального количества значащих бит **n** в **w** словах можно воспользоваться формулой:

### n = w\*5 +1, если n > 1
### n = 7, w = 1.

**Шаг 2.** Полученное на первом шаге количество слов необходимо перевести в единичную систему счисления и записать в первое слово, начиная со старшего бита. Далее следует нулевой бит, обозначающий конец записи количества слов. В зависимости от используемого числа слов, шаблон первого слова может выглядеть следующим образом:

| Количество слов | Код (шаблон) первого слова |
|-----------------|----------------------------|
| 2 | 110x xxxx |
| 3 | 1110 xxxx |
| 4 | 1111 0xxx |

С помощью «x» обозначены значащие биты.

**Шаг 3.** Остальные слова, используемые для кодирования позиции, будут иметь вид «10xx xxxx», где «10» — биты признака продолжения, а «x» — значащие биты.

**Шаг 4.** Далее осталось записать двоичное представление кодовой позиции в значащие биты полученных шаблонов. Заполнять необходимо с последнего слова, справа налево. Заполнение нужно начать с младших битов кодовой позиции, помещая их в младшие биты последнего слова. Если в первом слове остаются незанятые биты, то они заполняются нулями.

3.3. UTF-8. Пример кодирования
------------------------------

В качестве примера закодируем знак「や」— «я» из японской хираганы. Он имеет кодовую позицию U+3084.

Шаг 1. В бинарном представлении числа 3084₁₆ 14 значащих бит: 11 0000 1000 0100. Следовательно, для кодирования позиции необходимо 3 слова.

Шаг 2-3. Первое слово имеет вид «1110xxxx», два следующих — «10xxxxxx».

Шаг 4. Подставляем значащие биты позиции в выбранный шаблон. <span style="color:#99cc99">Зеленым</span> цветом веделены значащие биты, <span style="color:#990000">красным</span> — биты шаблона:

<span style="color:#990000">1110</span>00<span style="color:#99cc99">11</span> <span style="color:#990000">10</span><span style="color:#99cc99">000010</span> <span style="color:#990000">10</span><span style="color:#99cc99">000100</span>

Готово! Позиция U+3084 успешно закодирована.

3.4. UTF-16. Общие сведения
---------------------------

В отличие от UTF-8, для кодирования позиций в UTF-16 используются двухбайтовые слова. Здесь и далее будет подразумеваться, что одно слово состоит из 16 бит, если не сказано иначе.

Закодированная при помощи UTF-16 позиция представляется одним словом, если она лежит в базовой мультиязыковой плоскости, либо представляется двумя двухбайтовыми словами, если она размещается далее.

Алгоритм кодирования варьируется в зависимости от того, в каком диапазоне лежит кодовая позиция.

Если позиция находится в диапазоне от U+0000 до U+D7FF, либо от U+E000 до U+FFFF (напомним, что BMP заканчивается на кодовой позиции U+FFFF), то для кодирования в UTF-16 достаточно представить ее в двоичной системе счисления и поместить в двухбайтовое слово.

3.5. UTF-16. Суррогатные пары
-----------------------------

«Отлично, но почему из BMP вырезан диапазон кодовых позиций от U+D800 до U+DFFF, их что, нельзя закодировать одним словом?» — спросите вы. Так как произвольный символ с кодовой позицией от U+10000 и более невозможно представить с помощью одного слова, он представляются в виде пары слов, которые в результате кодирования будут лежать как раз в этом «вырезанном» промежутке. _Пара_ таких слов, кодирующих _одну_ кодовую позицию, называется **суррогатной**.

Принадлежность кода интервалу U+D800 до U+DFFF говорит, что данное слово не нужно рассматривать в отдельности, а интерпретировать как составную часть _пары_ слов, представляющую _одну_ кодовую позицию. Именно для этого нужен диапазон суррогатных пар!

Кроме того, диапазон суррогатных пар делится еще на 2 области: от U+D800 до U+DBFF — **верхние суррогаты** и от U+DC00 от U+DFFF — **нижние суррогаты**. Первое слово суррогатной пары всегда находится в диапазоне верхних суррогатов, а второе — в диапазоне нижних.

3.6. UTF-16. Алгоритм кодирования суррогатной пары
--------------------------------------------------

**Шаг 1.** Прежде всего, диапазон кодируемых значений «сдвигается» до нуля — для этого от кодовой позиции отнимается 10000₁₆. Как результат, в бинарном представлении позиции значащими остаются максимум 20 бит из 32, а полученное промежуточное значение лежит в диапазоне от 0₁₆ до FFFF₁₆.

**Шаг 2.** Сформируем шаблоны первого и второго слов. Начиная со старшего бита, в оба слова записывается последовательность 11011 — это индикатор суррогата. Затем за индикатором суррогата записывается либо 0, если слово первое, либо 1, если слово второе:

1101 10xx xxxx xxxx  1101 11xx xxxx xxxx

**Шаг 3.** Теперь необходимо взять промежуточный результат, полученный на шаге 1, и записать 10 младших бит во второе слово, а 10 старших — в первое.

3.7. UTF-16. Пример кодирования суррогатной пары
------------------------------------------------

Как пример, закодируем символ 🐕 — это собака из эмоджи. Ее кодовая позиция — U+1F415.

**Шаг 1.** Арифметический сдвиг до нуля:

1F415₁₆ - 10000₁₆ = F415₁₆

При переводе в двоичную СС промежуточного результата получаем:

1111 0100 0001 0101

**Шаг 2-3.** Подставим значащие биты в шаблон суррогатной пары. <span style="color:#99cc99">Зеленым</span> цветом выделены значащие биты, красным — биты шаблона:

<span style="color:#990000">110110</span>00 00<span style="color:#99cc99">111101</span>  <span style="color:#990000">110111</span><span style="color:#99cc99">00 00010101</span>

Пёсель успешно закодирован!

3.8. UTF-16. Порядок байт и BOM
-------------------------------

Еще один нюанс касательно кодировки UTF-16. Она существует в двух вариантах: UTF-16BE и UTF-16LE.

Исторически сложилось, что на всех платформах минимальной адресуемой единицей памяти является байт, а биты в нем нумеруются справа налево. Но в каком порядке нумеруются _байты_ машинного слова? В этом производители устройств соглашения не достигли. Слово в UTF-16 занимает больше одного байта — следовательно, не ясно, в каком байте хранятся старшие разряды, а в каком — младшие.

Если байты нумеруются справа налево от младшего к старшему, так же, как и биты, это **прямой порядок байт** (англ. _Big Endian, BE_). В противоположном случае порядок байт **обратный** (англ. _Little Endian, LE_). UTF-16BE и UTF-16LE производят кодирование с прямым и с обратным порядком байт соответственно.

В примере кодирования собаки 🐕 использовался прямой порядок байт:

| Номер слова | 1 | 1 | 0 | 0 |
|-------------|---|---|---|---|
| Номер байта | 1 | 0 | 1 | 0 |
| Значение₂   | 11011000 | 00111101 | 11011100 | 00010101 |
| Значение₁₆  | 0xD8 | 0x3D | 0xDC | 0x15 |

Если бы при кодировании использовался обратный порядок байт, то закодированная кодовая позиция в памяти выглядела так:

| Номер слова | 1 | 1 | 0 | 0 |
|-------------|---|---|---|---|
| Номер байта | 0 | 1 | 0 | 1 |
| Значение₂   | 00111101 | 11011000 | 00010101 | 11011100 |
| Значение₁₆  | 0x3D | 0xD8 | 0x15 | 0xDC |

Чтобы обозначить, какой порядок байт используется в файле или потоке данных, — LE или BE — при кодировании первым символом записывается метка порядка байт (англ. _Byte Order Mark, BOM_) U+FEFF. А перед декодированием производится проверка, какое слово прочиталось — 0xFEFF или 0xFFFE. В первом случае всё хорошо: порядок байт тот же, что и у нашей машины. А если метка порядка байт инвертирована, для работы с кодовыми позициями придется производить перестановку байт в каждом слове.