2. Что такое Unicode
====================

Unicode — это _стандарт_, определяющий огромную систему кодов символов и набор кодировок, задающий их представление в памяти.

2.1. Кодовое пространство Unicode
---------------------------------

В стандарте вводятся понятия **кодового пространства Unicode** (англ. _Unicode code space_) и **кодовой позиции** (англ. _code point, code position_).

**Кодовое пространство Юникода** — диапазон целых неотрицательных чисел (как правило, записывающихся в шестнадцатеричной системе счисления) от 0 до 10FFFF. Для сокращения далее будем называть его просто кодовым пространством.

**Кодовая позиция** — это значение из кодового пространства, однозначно соответствующее закрепленному за ним символу. Обычно позиция записывается с префиксом U+:

| Кодовая позиция | Символ |
|-----------------|--------|
| U+732B          | 猫     |
| U+1F408         | 🐈     |
| U+2211          | ∑      |

Согласно политике Консорциума, как только символу в стандарте присваивается определенная кодовая позиция, он никогда оттуда не исчезнет, а его кодовая позиция навсегда останется прежней.

Таким образом, благодаря единому кодовому пространству для всех письменностей, можно создать текстовый документ, одновременно сочетающий в себе любые символы и языки.

Для удобства всё кодовое пространство разделено на плоскости под номерами от 0 до 16 (англ. _planes_), в каждой из которых содержится по 2¹⁶ — 65 536 символов.

| Плоскость | Название                                 | Диапазон символов|
|-----------|------------------------------------------|------------------|
| 0         | Basic multilingual plane, BMP            | U+0000…U+FFFF    |
| 1         | Supplementary multilingual plane, SMP    | U+10000…U+1FFFF  |
| 2         | Supplementary ideographic plane, SIP     | U+20000…U+2FFFF  |
| 3         | Tertiary ideographic plane, TIP          | U+30000…U+3FFFF  |
| 4-13      | unassigned                               | U+40000…U+DFFFF  |
| 14        | Supplementary special-purpose plane, SSP | U+E0000…U+EFFFF  |
| 15-16     | Supplementary private use area, SPUA-A/B | U+F0000…U+10FFFF |

Алфавиты всех современных языков, а также кодовые позиции наиболее употребимых символов содержатся в базовой мультиязыковой плоскости (англ. _Basic Multilingual Plane_, сокр. _BMP_) — это плоскость под номером 0. Как оказалось, 65 536 кодовых позиций достаточно, чтобы закодировать все европейские, ближневосточные языки, современную письменность азиатских и африканских языков. Первые 128 кодовых позиций данной плоскости в точности соответствуют кодам ASCII.

Чуть реже используется первая — **дополнительная мультиязыковая плоскость** (англ. _Supplementary Multilingual Plane_, сокр. _SMP_). По большей части она содержит коды различных исторических письменностей. Но особенно SMP примечательна тем, что содержит эмоджи, которые нужно корректно обрабатывать наравне с другими письменностями.

2.2. Разновидности кодовых позиций
----------------------------------

В стандарте все кодовые позиции подразделяются на семь базовых типов (англ. _code point basic type_):

**Graphic.** К этому типу относятся любые символы, имеющие графическое изображение.

**Format.** Символы, которые не имеют своего изображения, но влияют на отображение окружающих символов. К этой категории относятся символы переноса строки и абзацные отступы.

**Control.** К данному типу относятся управляющие символы — их назначение определяется протоколами и стандартами, выходящими за рамки стандарта Unicode.

**Private-use.** Использование символов этого типа определяется частными соглашениями, выходящими за рамки стандарта Unicode. Символы могут использоваться для любых целей, но для успешного обмена данными отправитель и получатель должны договориться об их однозначной интерпретации. Стандарт обязуется никогда не присваивать значения этим кодовым позициям.

**Surrogate.** В этой категории находятся кодовые позиции, использующиеся для представления суррогатных пар UTF-16. Подробно про UTF-16 и суррогатные пары рассказано в разделе 3.4 и далее.

**Noncharacter.** Кодовые позиции этого типа не используются для кодирования символов. Они предназначены для внутреннего использования в программной реализации стандарта: например, в качестве разделителей или флагов. Назначение каждого конкретного символа определяется разработчиками в зависимости от контекста использования.

**Reserved.** Кодовые позиции, которым стандартом еще не присвоен какой-либо символ (но всё ещё может измениться!).

Помимо классификации по типу, есть разделение кодовых позиций по непересекающимся основным категориям (англ. _general category_). Всего их семь: **Letter**, **Mark**, **Number**, **Punctuation**, **Symbol**, **Separator**, **Other**. Символы из первых четырех категорий относятся только к типу Graphic.


Из приведенных классификаций видно, что ставить знак эквивалентности между символом (в традиционно-наивном его понимании) и кодовой позицией не стоит. Кодовая позиция не всегда соответствует букве, знаку препинания или цифре. Но есть еще одна тонкость.

В стандарте акцентируется внимание на том, что символы графического типа могут быть **базовыми** (англ. _base characters_) и **комбинируемыми** (англ. _combining characters_). Формально, комбинируемые символы — это все те, которые относятся к категории Mark (все остальные символы типа Graphic относятся к базовым). Комбинируемые символы используются для визуального изменения базовых символов (например, для добавления [диакритики](https://ru.wikipedia.org/wiki/%D0%94%D0%B8%D0%B0%D0%BA%D1%80%D0%B8%D1%82%D0%B8%D1%87%D0%B5) или изменения направления письма) и не могут встречаться самостоятельно, а только в сочетании с базовыми:

## a + ̈ = ä

Несмотря на то, что для немецкой а-умлаут существует отдельная кодовая позиция (U+00E4), ее можно представить не как отдельный символ, как комбинацию латинской а (U+0061) и умлаута (U+0308). За базовым символом может следовать как и один комбинируемый, так и несколько, и все они суммарно будут видоизменять отображение предшествующего базового символа.

2.3. Unicode Character Database
-------------------------------

Стандарт закрепляет за каждым символом не только кодовую позицию: в некоторых случаях для обеспечения удобочитаемости текста необходимо не только знать, какой символ за ней скрывается, но учитывать его свойства.

Для того, чтобы собрать вместе характеристики каждой кодовой позиции, была разработана [база данных символов Unicode](https://www.unicode.org/ucd/) (англ. _Unicode Character Database, UCD_). UCD представляет собой набор файлов, где присваиваются свойства каждой кодовой позиции. Для удобства парсинга и уменьшения объема занимаемой памяти в UCD для полей свойств и их значений используются алиасы.

Вот некоторые примеры свойств, которые содержит база данных:
* уникальное имя;
* кодовая позиция;
* основная категория;
* язык и система письменности;
* другие характеристики (является ли пробелом, тире, идеограммой или символом алфавита, устаревшим символом и т.д.);
* свойства, связанные с отображением (возможное направление письма, отражение, ширина и т.д.);
* регистр (нижний/верхний, выравнивание регистра).

2.4. Unicode Character Code Charts
----------------------------------

Чтобы узнать, как выглядит символ с некоторой кодовой позицией, можно воспользоваться специально созданными **таблицами кодов символов Unicode** (англ. _Unicode Character Code Charts_). Под них выделен [раздел](https://www.unicode.org/charts/) на сайте Unicode, в котором можно воспользоваться либо поиском по значению кодовой позиции, либо найти символ вручную — они сгруппированы по разделам “Scripts” и “Symbols and Punctuation”. В таблицах содержатся не только образцы начертания символов, но и их уникальные имена, а также небольшие комментарии к некоторым символам. Например, могут быть указаны кодовые позиции, комбинацией которых может являться обозреваемый символ, либо семантическое значение и варианты произношения для идеограмм.